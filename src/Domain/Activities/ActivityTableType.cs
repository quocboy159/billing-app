namespace Domain.Activities;

public enum ActivityTableType
{
    Invoice = 100,
    Estimate = 200,
    Recurring = 300,
    Bill = 400,
    RecurringBill = 500,
    PurchaseOrder = 600,
    Banks = 800,
    Reconciliation = 900,
    CreditNote = 1800
}
