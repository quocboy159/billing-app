namespace SharedKernel;

//TODO: Will hanlder later
/// <summary>
/// A domain event is a "something interesting happened to an aggregate" notification,
/// dispatched after the aggregate is persisted
/// </summary>
public interface IDomainEvent;
